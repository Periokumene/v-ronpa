import { gameACharacterSmokeLaunchDefinition } from "./gameATestEntries";

/** Dedicated Alice visual-smoke launch definition; never selected in production. */
export const gameALaunchDefinition = gameACharacterSmokeLaunchDefinition;
