import { gameACharacterSmokeStoryDefinition as testStoryDefinition } from "./gameATestEntries";

/** Dedicated Alice visual-smoke story definition; never selected in production. */
export const gameAStoryDefinition = testStoryDefinition;
