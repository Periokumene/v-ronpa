import { gameAOpeningLaunchDefinition } from "./gameAScripts";

/** Canonical launch definition selected at build time by the Vite mode. */
export const gameALaunchDefinition = gameAOpeningLaunchDefinition;
