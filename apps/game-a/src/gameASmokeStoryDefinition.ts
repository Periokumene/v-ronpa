import { gameASmokeStoryDefinition as testStoryDefinition } from "./gameATestEntries";

/** Dedicated Playwright smoke story definition; never selected in production. */
export const gameAStoryDefinition = testStoryDefinition;
