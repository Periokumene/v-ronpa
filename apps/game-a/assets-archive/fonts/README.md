# Archived Game A Fonts

`jinghua-laosong-gb.woff2` and `fusion-pixel-10px-proportional-zh-hans.woff2` are retained for visual comparison during the Fusion Pixel Font evaluation. They are intentionally outside the public runtime `fonts/` directory, so they are not emitted into Game A's `ContentManifest` or preloaded by the font runtime.
